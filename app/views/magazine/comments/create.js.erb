var $form = $("form#new_article_comment");
<% if @comment.save %>
$("#comments").prepend("<%= escape_javascript(render 'comment', :comment => @comment) %>");
  $form.get(0).reset();
<% else %>
  $form.html("<%= escape_javascript(render('form')) %>");
<% end %>
